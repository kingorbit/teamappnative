// styles.js

const styles = {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#9091fd', // Ustawienie koloru tła
  },
  menuItem: {
    padding: 20,
    margin: 10,
    backgroundColor: 'white', // Tło przycisku
    borderRadius: 10, // Zaokrąglenie krawędzi
    alignItems: 'center',
    justifyContent: 'center',
    elevation: 4, // Cień przycisku
  },
  menuItemText: {
    fontSize: 18,
    fontWeight: 'bold',
  },
};

export default styles;
